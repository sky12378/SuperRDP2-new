# Build / runtime artifacts (not source)
*.log
*.bak
*.autobak
*.ini.new
SuperRDP2-new-x64-*.zip

# WorkBuddy internal memory / agent state (not project source)
.workbuddy/

# OS junk
Thumbs.db
ehthumbs.db
Desktop.ini
